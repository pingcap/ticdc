# diff Configuration.

check-thread-count = 4

export-fix-sql = true

check-struct-only = false

[task]
    output-dir = "/tmp/tidb_cdc_test/sync_diff/output"

    source-instances = ["up"]

    target-instance = "down"

    target-check-tables = ["test.*"]

[data-sources]
[data-sources.up]
    host = "10.2.15.7"
    port = 4000
    user = "root"
    password = ""

[data-sources.down]
    host = "10.2.15.195"
    port = 4000
    user = "root"
    password = ""
